# Источник данных: mock (демо, по умолчанию) | api (реальный backend)
VITE_DATA_SOURCE=mock

# Необязательно (по умолчанию — относительные пути через Vite-прокси / nginx):
# VITE_API_URL=/api
# VITE_WS_URL=ws://localhost:4000/ws
# VITE_SYMBOL=EURUSD
